import React from 'react';

const Ticket = ({ ticket }) => {
    return (
        <div className="ticket">
            {/* Отобразите информацию о билете, например, цену, маршрут, и т. д. */}
        </div>
    );
};

export default Ticket;
